# Repo root — keep secrets out of git
# (alphanet-core removed)
**/.env
!**/.env.example


# venvs / node / build
.venv/
node_modules/
*.db
consumer/.env
polymarket-sentiment-agent/backend/.env
.DS_Store
__MACOSX/
.venv-backend/
.venv-chf/

# Editor/agent scaffolding — not needed by the app, and has previously
# carried a third party's contact info (workshop agenda). Local-only.
.cursor/
**/.cursor/
docs/cdp-llms.txt
